

```

git rm --cached *src/fiber/demo.cr*     删除远程仓库的文件
git rm -r --cached */src* 删除目录
git add .
git commit  -m "first commit"
git remote add origin https://github.com/lgphp/crystal-example.git   将远端仓库与本地仓库建立链接
git push -u origin master   提交

```
